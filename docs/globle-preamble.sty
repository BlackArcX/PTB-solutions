% to allow preambles in included files
\usepackage[subpreambles=true]{standalone}
% to reduce page margin to 1 in
\usepackage[margin=1in]{geometry}
% for graphics
\usepackage{tikz}
% for equations
\usepackage{amsmath}
% for symbols like \because
\usepackage{amssymb}
% to import sub latex files
\usepackage{import}
% for sections related stuff
\usepackage{sectsty}

\usepackage{hyperref}
\hypersetup{colorlinks=true}

% remove pagraph start indentation
\setlength{\parindent}{0em}
% remove page numbers
\pagenumbering{gobble}

\makeatletter
\renewcommand{\@seccntformat}[1]{}
\makeatother

\subsubsectionfont{\sectionrule{0pt}{0pt}{-5pt}{0.8pt}}
\subsectionfont{\center}
